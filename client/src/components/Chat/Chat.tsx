import React, { useState, useEffect, useRef } from 'react';
import { Button, Dropdown, Menu } from '@arco-design/web-react';
const Chat: React.FC = () => {
  useEffect(() => {});
  return <div className="w-full h-[300px] p-[20px] absolute bottom-0">世界</div>;
};
export default Chat;
